spring:
  datasource:
    url: jdbc:mysql://172.17.0.1:3306/calculator?serverTimezone=UTC
    username: root
    password: my-secret-pw

  jpa:
    hibernate:
      ddl-auto: update

  #disable liquibase at start-up
  liquibase:
    enabled: false

server:
  port: 8888